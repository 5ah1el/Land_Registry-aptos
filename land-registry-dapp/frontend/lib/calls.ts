/* import { aptos, MODULE_ADDRESS } from "./aptos";

export async function viewProperty(parcelId: string) {
  return aptos.view({
    payload: {
      function: `${MODULE_ADDRESS}::property_registry::get_property_info`,
      arguments: [parcelId],
    },
  });
}
 */